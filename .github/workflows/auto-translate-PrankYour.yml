name: Auto Translate

on:
  push:
    branches: ["main"]
    paths:
      - "lang/PrankYour/en.json"
      - ".github/workflows/auto-translate-PrankYour.yml"
  workflow_dispatch:

concurrency:
  group: "AutoTranslatePrankYour"
  cancel-in-progress: true

jobs:
  AutoTranslate:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v3
      - name: Run Auto Translate PrankYour
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
      - run: npm i
      - run: |
          for lang in 'zh-CN' 'zh-TW' 'hi' 'es' 'fr' 'ar' 'bn' 'ru' 'pt' 'id' 'ha' 'pa' 'de' 'ja' 'fa' 'sw' 'vi' 'te' 'it' 'jw' ;
            do
              node translate-PrankYour.js $lang
            done

      - name: Import GPG key
        uses: crazy-max/ghaction-import-gpg@v5
        with:
          gpg_private_key: ${{ secrets.GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.PASSPHRASE }}
          git_user_signingkey: true
          git_commit_gpgsign: true

      - name: Push to Github
        run: |
          git config --global user.name "PrankGGBot"
          git config --global user.email "${{secrets.GH_BOT_EMAIL}}"
          git add -A
          git commit -m "Update PrankYour Translations from ${{ github.event.head_commit.id }}"
          git push
